function channels = filter_channels(labels, eeg)

% labels: a 1 x num_channels cell containing channel names
% eeg: num_channels x size array of doubles, given size = seconds x frequency.

% TODO: implement a real channel filter function...
% channels should be a 1 x num_channels cell array containing the labels of the channels to retain.
channels = cell(1,1);


end

